// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line
let copyFixedValue = fixedValue + 1;
 return copyFixedValue;
  // Only change code above this line
}